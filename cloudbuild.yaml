steps:
  # SSH into the instance
  - name: 'gcr.io/cloud-builders/gcloud'
    args: ['compute', 'ssh', 'suhailakofficial@first', '--zone=us-central1-a', '--command', 'sudo mkdir /var/test']

  # Confirm directory creation
  - name: 'gcr.io/cloud-builders/gcloud'
    args: ['compute', 'ssh', 'suhailakofficial@first', '--zone=us-central1-a', '--command', 'ls -l /var']
